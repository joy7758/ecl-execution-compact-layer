# ECL Dependency SDK v0.1

## 1. 3-line Integration

```python
import ecl_dependency as ecl

ecl.wrap(trace)
ecl.verify(ecl_object)
```

## 2. Concept Statement

ECL can be optionally imported as a deterministic execution representation layer.

## 3. Non-intrusive Guarantee

- does not modify host runtime
- does not override execution
- does not require framework changes

